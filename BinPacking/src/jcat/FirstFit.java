package jcat;

public class FirstFit {

	void algorithm() {
		int numBins = 10;
		int numBlocks = 25;
		double totals[] = new double[numBins];
		for (int b = 0; b < numBlocks - 1; b++) {
//			double amt = nextBlock();
//			int bin = 0;
//			while (bin < numBins) {
//				z.probe(bin);
//				if (totals[bin++] + amt <= 1.0)
//					break;
//			}
//			if (bin == numBins)
//				break;
//			totals[bin] += amt;
		}
	}

}